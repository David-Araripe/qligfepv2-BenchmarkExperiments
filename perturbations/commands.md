# bace

```bash
setupFEP -FF AMBER14sb -c TETRA -r 25 -b auto --start 0.5 -R 10 -S sigmoidal -ts 2fs -clean dcd -j lomap.json -log debug -rest hybridization_ls
```

# bace_hunt

```bash
setupFEP -FF AMBER14sb -c TETRA -r 25 -b auto --start 0.5 -R 10 -S sigmoidal -ts 2fs -clean dcd -j lomap.json -log debug -rest hybridization_ls
```

# bace_p2

```bash
setupFEP -FF AMBER14sb -c TETRA -r 25 -b auto --start 0.5 -R 10 -S sigmoidal -ts 2fs -clean dcd -j lomap.json -log debug -rest hybridization_ls
```

# cdk2

```bash
setupFEP -FF AMBER14sb -c TETRA -r 25 -b auto --start 0.5 -R 10 -S sigmoidal -ts 2fs -clean dcd -j lomap.json -log debug -rest hybridization_ls
```

# cdk8

```bash
setupFEP -FF AMBER14sb -c TETRA -r 25 -b auto --start 0.5 -R 10 -S sigmoidal -ts 2fs -clean dcd -j lomap.json -log debug -rest hybridization_p
```
# galectin

```bash
setupFEP -FF AMBER14sb -c TETRA -r 25 -b auto --start 0.5 -R 10 -S sigmoidal -ts 2fs -clean dcd -j lomap.json -log debug -rest 
```
# jnk1

```bash
setupFEP -FF AMBER14sb -c TETRA -r 25 -b auto --start 0.5 -R 10 -S sigmoidal -ts 2fs -clean dcd -j lomap.json -log debug -rest 
```
# p38

```bash
setupFEP -FF AMBER14sb -c TETRA -r 25 -b auto --start 0.5 -R 10 -S sigmoidal -ts 2fs -clean dcd -j lomap.json -log debug -rest 
```
# pde2

```bash
setupFEP -FF AMBER14sb -c TETRA -r 25 -b auto --start 0.5 -R 10 -S sigmoidal -ts 2fs -clean dcd -j lomap.json -log debug -rest 
```
# ptp1b

```bash
setupFEP -FF AMBER14sb -c TETRA -r 25 -b auto --start 0.5 -R 10 -S sigmoidal -ts 2fs -clean dcd -j lomap.json -log debug -rest 
```
# syk

```bash
setupFEP -FF AMBER14sb -c TETRA -r 25 -b auto --start 0.5 -R 10 -S sigmoidal -ts 2fs -clean dcd -j lomap.json -log debug -rest 
```
# tnks2

```bash
setupFEP -FF AMBER14sb -c TETRA -r 25 -b auto --start 0.5 -R 10 -S sigmoidal -ts 2fs -clean dcd -j lomap.json -log debug -rest 
```
# bace_hunt

```bash
setupFEP -FF AMBER14sb -c TETRA -r 25 -b auto --start 0.5 -R 10 -S sigmoidal -ts 2fs -clean dcd -j lomap.json -log debug -rest 
```
# cdk2

```bash
setupFEP -FF AMBER14sb -c TETRA -r 25 -b auto --start 0.5 -R 10 -S sigmoidal -ts 2fs -clean dcd -j lomap.json -log debug -rest 
```
# cmet

```bash
setupFEP -FF AMBER14sb -c TETRA -r 25 -b auto --start 0.5 -R 10 -S sigmoidal -ts 2fs -clean dcd -j lomap.json -log debug -rest 
```
# eg5

```bash
setupFEP -FF AMBER14sb -c TETRA -r 25 -b auto --start 0.5 -R 10 -S sigmoidal -ts 2fs -clean dcd -j lomap.json -log debug -rest 
```
# hif2a

```bash
setupFEP -FF AMBER14sb -c TETRA -r 25 -b auto --start 0.5 -R 10 -S sigmoidal -ts 2fs -clean dcd -j lomap.json -log debug -rest 
```
# mcl1

```bash
setupFEP -FF AMBER14sb -c TETRA -r 25 -b auto --start 0.5 -R 10 -S sigmoidal -ts 2fs -clean dcd -j lomap.json -log debug -rest 
```
# pde10

```bash
setupFEP -FF AMBER14sb -c TETRA -r 25 -b auto --start 0.5 -R 10 -S sigmoidal -ts 2fs -clean dcd -j lomap.json -log debug -rest 
```
# pfkfb3

```bash
setupFEP -FF AMBER14sb -c TETRA -r 25 -b auto --start 0.5 -R 10 -S sigmoidal -ts 2fs -clean dcd -j lomap.json -log debug -rest 
```
# shp2

```bash
setupFEP -FF AMBER14sb -c TETRA -r 25 -b auto --start 0.5 -R 10 -S sigmoidal -ts 2fs -clean dcd -j lomap.json -log debug -rest 
```
# thrombin

```bash
setupFEP -FF AMBER14sb -c TETRA -r 25 -b auto --start 0.5 -R 10 -S sigmoidal -ts 2fs -clean dcd -j lomap.json -log debug -rest 
```
# tyk2

```bash
setupFEP -FF AMBER14sb -c TETRA -r 25 -b auto --start 0.5 -R 10 -S sigmoidal -ts 2fs -clean dcd -j lomap.json -log debug -rest 
```